# PIS-IN AOI AI 智能质检项目

## 面试准备总手册

> 适用岗位：AI 应用工程师、AI 产品经理、智能制造/视觉质检解决方案岗位
>
> 技术边界：当前采用 AOI 单机独立运行，不接入自动化 Handler。技术栈沿用 PIS-IN AOI、YOLOv8、TensorRT 适配边界、FastAPI、PostgreSQL、React、Docker Compose 与独立 Agent/RAG 服务。文中将“已验证事实、历史成果、设计目标、待现场验收项”明确区分，避免面试时过度承诺。

---

## 0. 先记住项目边界

这个项目不是“用 YOLO 替换 AOI”，也不是自动化分选机控制项目，而是在一台 AOI 工控机上建设不干预原设备机械流程的 AI 复判系统：

```text
本机操作员 / PIS-IN AOI 软件触发
        |
        v
本机采集 2D 多光源 + 3D 量测 + AOI 结果 + Tray Map
        |
        v
数据关联校验 -> YOLOv8/TensorRT -> 3D/规则后融合 -> PASS/FAIL/REVIEW
        |
        +--> 本地看板/质量报表/工站异常预警
        +--> 本机人工复核、金标准回流、模型迭代
        +--> MES 异步同步（可选，不参与实时判定）
```

AI 服务异常时，原 AOI 继续工作；身份不明、文件不完整、未知缺陷和低置信度样本不能自动 PASS。Handler 接口研究成果保留为未来扩展，但通过 `HANDLER_INTEGRATION_ENABLED=false` 默认关闭。

### 0.1 架构类型怎么回答

```text
这是单机容器化的模块化服务架构，不是严格的全微服务架构。
FastAPI、领域模型、推理编排和数据访问构成模块化核心服务；
Agent/RAG 独立部署以隔离非实时任务；React/Nginx、PostgreSQL、simulator
和 API 通过 Docker Compose 运行在同一台工控机上。
```

这样设计的原因是单机 AOI 更看重低延迟、易部署、易排障和断网可运行。只有 Agent/RAG 这类异步、可降级模块单独拆服务，避免为了“微服务”而增加网络和运维复杂度。

### 0.2 技术栈统一口径

| 层级 | 技术 | 面试表达 |
|---|---|---|
| 设备与数据 | PIS-IN AOI、R/G/B/RING、3D、Tray Map | 本机采集，本机数据适配 |
| AI 推理 | YOLOv8s 基线、PyTorch/ONNX、TensorRT FP16 适配边界 | 真实 Engine 与精度待现场验证 |
| 后端 | FastAPI、Pydantic v2、SQLAlchemy 2、Alembic | 模块化核心服务与安全门禁 |
| 数据库 | PostgreSQL 16、pgvector；测试使用 SQLite | 业务数据与知识检索统一持久化 |
| Agent/RAG | 3 个领域工作流、证据检索、确定性 Provider | 只做分析和报告，不参与实时 PASS |
| 前端 | React 18、TypeScript、Vite、ECharts、Lucide | 8 个本地运营页面 |
| 部署 | Docker Compose、Nginx、健康检查、GPU overlay | 单机离线可运行，服务可独立重启 |

### 0.3 个人职责统一口径

1. 将误报、漏放、REVIEW、P95、持续吞吐和静默错配率转化为阶段验收指标。
2. 设计 Source Key、附件汇聚、事件状态机、隔离区和数据库幂等约束。
3. 推动 YOLOv8/TensorRT 适配、三态决策、人工复核、缺陷报表和预警闭环。
4. 负责 FastAPI 接口、Agent/RAG 故障隔离、Compose 部署和跨模块联调。
5. 协调算法、后端、前端、测试、实施运维及质量/设备相关人员完成阶段交付。

### 0.4 个人成果统一口径

- 完成可运行的单机 PoC，提供 8 个前端业务页面和本地质量闭环。
- API 63 条、Agent/RAG 6 条、simulator 2 条自动化测试通过；Handler 默认关闭用例已纳入回归。
- 建立多源数据不串件、缺失不放行、结果可追溯和模型可回退的安全基线。
- 完成 Compose 实际启动、PostgreSQL Alembic 迁移和服务健康检查验证。
- 真实 YOLO 精度、TensorRT P95、持续吞吐和现场误报率仍属于待现场验收项，不能写成已实现成果。

---

## 1. 项目定位与岗位映射

### 1.1 一句话定位

面向半导体封装产线 PIS-IN AOI 的多源数据复判与质量闭环系统，目标是减少传统规则误报、降低人工复检量，并把检测结果沉淀为可追溯的缺陷与工站运营数据。

### 1.2 AI 应用工程师的讲法

重点讲“模型如何可靠地进入产线”：

- 用确定性 Source Key 关联 2D 多光源图像、3D 量测、AOI 原始结果和 Tray/Slot 信息。
- 通过文件稳定性、字段完整性、采集时间、Hash 和幂等约束，处理乱序、重复、延迟、部分写入和设备重启。
- 依据 ROI 是否稳定选择分类或 YOLOv8 检测；3D 数值走后融合并保留物理规则护栏。
- 用 FastAPI 提供本地接口，用 TensorRT 适配边界支持边缘推理，用独立 Agent/RAG 服务辅助数据质量分析、复核和异常报告。
- 通过三态决策、降级模式、审计记录和回退机制保证质量边界。

### 1.3 AI 产品经理的讲法

重点讲“业务目标如何变成可交付和可验收的产品”：

- 将误报、人工复检、漏放、新机种导入和产线节拍转化为指标与阶段门禁。
- 设计“实时复判 + 数据资产 + 模型治理 + 质量运营”的产品闭环。
- 明确质量、设备、IT/MES、AI 的职责和接口先行机制。
- 规划缺陷分类报表、Tray Map 分布、工站高缺陷率预警、异常报告、人工复核回流和模型漂移监控。
- 以数据审计、离线 PoC、单机影子运行、受控单机上线四个阶段推进，不跳过盲测和回退演练。

---

## 2. 简历可直接使用的表述

### 2.1 AI 应用工程师版

```text
PIS-IN AOI 智能质检与误报过滤系统
针对半导体封装 AOI 误报高、人工复检量大和新机种导入慢的问题，设计单机 AI 复判系统。基于 FastAPI + YOLOv8 + TensorRT 适配边界构建本地推理服务，使用确定性 Source Key 关联 2D 多光源图像、3D 量测、AOI 结果和 Tray Map，采用 ROI 分类/YOLO 检测、3D 后融合和 PASS/FAIL/REVIEW 三态决策；使用独立 Agent/RAG 服务辅助人工复核、质量分析和异常报告。重点解决多源文件错配、异步到达、重复推理、模型版本共存、故障回退和单机上线验收问题。
```

### 2.2 AI 产品经理版

```text
负责 PIS-IN AOI AI 质检项目的需求拆解与落地规划，将误报率、人工复检量、漏放风险、导入周期和节拍转化为可验收指标；设计数据关联、模型决策、人工复核、缺陷报表、工站高缺陷率预警和异常报告闭环；组织设备、质量、IT/MES 与 AI 团队完成字段字典、缺陷字典、金标准和单机影子验证，形成从 PoC 到受控单机上线的交付基线。
```

### 2.3 量化结果的安全写法

只有具备现场验收记录、样本量和统计口径时，才写“已实现”：

```text
历史项目成果：综合误报率 42% -> 9%，新机种导入 2-3 天 -> 4 小时，单组件模型推理 <50ms。
```

如果当前版本仍处于方案、PoC 或影子运行阶段，写成：

```text
完成数据适配、模型链路、影子运行和生产验收设计；误报过滤率、漏放率、端到端 P95 和持续吞吐等待现场基线化验证。
```

不要把设计目标直接写成生产成果，也不要把单张模型耗时写成整条产线延迟。

---

## 3. 面试自我介绍模板

### 3.1 30 秒版本

```text
我做的是半导体封装 AOI 的 AI 旁路复判。PIS-IN 原 AOI 继续负责基础检测和机械流程，AI 主要复判 NG 和低置信度样本，目标是减少误报和人工复检。系统把 2D 多光源、3D 量测、AOI 结果和 Tray Map 用确定性 Source Key 关联，经过 YOLOv8/TensorRT 推理、3D 后融合和三态决策输出 PASS、FAIL、REVIEW。我的重点不只是模型精度，还包括数据不串件、服务可降级、结果可追溯和从 PoC 到产线的验收门禁。
```

### 3.2 90 秒版本

```text
项目背景是 PIS-IN AOI 在材料、焊盘和光照波动下误报较多，人工复检成本高，新机种导入也依赖大量规则调参。我的方案采用单机独立运行，不修改原设备机械和基础判定逻辑。实时链路由本机数据适配、输入质量校验、YOLOv8/TensorRT、3D 后融合和 PASS/FAIL/REVIEW 决策组成；异步链路由独立 Agent/RAG 服务辅助人工复核、知识检索和异常报告。

其中最关键的是数据关联。不能按文件修改时间拼数据，而要用设备 ID、会话、检测序号、Tray、Slot 和正背面组成 Source Key，R/G/B/RING 等光源作为同一事件的附件。文件乱序、重复、延迟或身份缺失时，系统等待、重试或隔离，不能靠邻近文件补位。上线按数据审计、离线 PoC、单机影子运行和受控单机上线逐步推进，最终同时看缺陷召回、自动 PASS 漏放率、误报过滤率、REVIEW 比例、端到端 P95、持续吞吐和静默错配率。
```

### 3.3 3 分钟版本的叙事顺序

```text
业务痛点 -> 为什么旁路 -> 数据如何对齐 -> 模型如何选择 -> 服务如何部署
-> 如何处理异常 -> 如何验收上线 -> 产生什么业务价值
```

面试官打断时，优先回到“质量边界、可追溯和回退”三个关键词。

---

## 4. 架构与数据流怎么讲

### 4.1 六层架构

| 层级 | 项目内容 | 面试重点 |
|---|---|---|
| 设备层 | PIS-IN 2D 多光源、3D 量测、AOI 原始结果、Tray Map | 不改机械流程，旁路接入 |
| 数据层 | 文件监听/适配、Source Key、事件状态、附件、隔离区 | 先证明输入属于正确组件 |
| 算法层 | ROI 分类或 YOLOv8 检测、光源筛选、3D 后融合 | 模型由 ROI 和验收指标决定 |
| 服务层 | FastAPI、TensorRT 常驻引擎、超时/限流/健康检查 | 看端到端 P95 和持续吞吐 |
| 治理层 | 独立 Agent/RAG、人工复核、金标准、评估、审批、影子、回退 | 异步隔离，允许人工介入和版本审计 |
| 运营层 | 缺陷报表、工站预警、异常报告、漂移监控 | 从一次判定扩展到质量闭环 |

### 4.2 单事件处理顺序

```text
文件发现
 -> 等待文件稳定
 -> 解析设备字段
 -> 生成 source_key_hash
 -> 事务 Upsert inspection_events
 -> 写入 data_attachment_links
 -> 校验 required_light_set/3D/AOI/时间偏差/Hash
 -> READY/VALIDATED
 -> YOLO/TensorRT 推理
 -> 3D 与物理规则后融合
 -> PASS/FAIL/REVIEW
 -> 写 inference_results、Tray Map、MES 和报表
```

### 4.3 数据关联原则

- `source_key_hash` 代表一次物理检测事件；不能把 LightGroup 放进事件主键。
- R/G/B/RING/IR/UV 是附件属性，同一组件共享一个 `event_uuid`。
- `inference_results` 按 `event_uuid + model_version` 唯一，支持新旧模型结果共存。
- 身份缺失进入 `quarantine_events`，数据不完整或错位进入 REVIEW/INVALID/QUARANTINED。
- Redis 若部署，只用于缓存或削峰，数据库约束才是最终幂等事实来源。

---

## 5. 模型方案的专业表达

### 5.1 什么时候用分类，什么时候用 YOLO

| 场景 | 首选 | 原因 |
|---|---|---|
| AOI 已提供稳定、可信 ROI，每个 ROI 通常一个缺陷状态 | 轻量 ROI 分类 | 结构简单、速度快、阈值易校准 |
| ROI 不稳定、一个视野可能有多个缺陷、需要定位 | YOLOv8 检测 | 同时输出类别、置信度和边界框 |
| 需要区分多个候选区域且边缘轮廓很关键 | YOLO 检测或分割对照实验 | 以盲测结果而非模型名称决定 |

### 5.2 11 组光源如何落地

YOLOv8 不是原生 11 通道模型。实施顺序应是：

1. 审计 11 组光源的到达率、曝光稳定性、缺陷可分性和互补性。
2. 做单光源/组合光源消融，先筛选 3-4 组有效通道，组成标准 3 通道输入。
3. 若召回明显下降，再比较四通道首层改造、结果级融合或特征级融合。
4. 以缺陷召回、自动 PASS 漏放、显存和持续吞吐共同决定最终方案。

不要在面试中说“YOLO 直接支持 11 通道”，这会被立即追问输入层和 TensorRT 部署细节。

### 5.3 3D 数据怎么用

球高、共面性、翘曲等是数值特征，不与 2D 像素同构。推荐采用：

```text
YOLO 2D 证据 + 3D 数值特征 + 物理阈值护栏 -> 决策层
```

3D 硬阈值触发高风险缺陷时直接 FAIL；3D 缺失则进入独立的 `2D_ONLY` 模式，默认不自动 PASS，除非独立盲测证明质量边界仍满足要求。

---

## 6. 从方案到生产的实施路线

| 阶段 | 主要工作 | 进入条件 | 退出/验收条件 | 产物 |
|---|---|---|---|---|
| 数据审计 | 确认文件、字段、时间源、身份键、保存策略 | 设备工程师可提供连续样例 | 可验证事件 Source Key 一致率 100%，身份缺失可隔离 | 字段字典、样本清单、故障清单 |
| 离线 PoC | 清洗标注、光源消融、模型训练、阈值校准 | 有覆盖产品/缺陷/批次的样本 | 独立盲测达到质量门槛，2D/3D 方案定型 | 模型、阈值、评估报告 |
| 影子运行 | AI 只记录建议，不控制产线 | 数据关联和回写链路可用 | 连续运行无静默错配；P95、吞吐、积压可解释 | 差异报表、回退演练记录 |
| 受控单机上线 | 在本机按产品/缺陷类别开放自动 PASS | 质量、设备、IT/MES、AI 联合审批 | 漏放率、稳定性、告警和回退持续达标 | 发布包、上线记录、SOP |

### 6.1 生产准入硬门槛

1. 静默错配率为 0，并有乱序、重复、延迟、断网、重启、部分写入故障注入证据。
2. 自动 PASS 漏放率按产品和缺陷类别达标，并报告样本量和置信区间。
3. 峰值负载下持续吞吐不低于工站需求，端到端 P95 不造成队列持续增长。
4. FastAPI、TensorRT、数据库或网络异常时，原 AOI 仍可继续生产。
5. 模型、阈值、策略和自动化范围有版本号、审批记录和可回退包。

---

## 7. 指标、证据和价值

### 7.1 需要同时报告的指标

| 指标 | 含义 | 面试时的解释 |
|---|---|---|
| 误报过滤率 | 原 AOI 误报中被 AI 正确过滤的比例 | 衡量减少人工复检的直接收益 |
| 自动 PASS 漏放率 | 真实缺陷被 AI 自动放行的比例 | 生产自动化的首要质量门禁 |
| 缺陷召回率 | 各缺陷类别被正确识别的比例 | 必须分产品、设备、批次报告 |
| REVIEW 比例 | 进入人工复核的样本占比 | 安全和收益之间的平衡指标 |
| 端到端 P95 | 文件读取到结果回写的 95 分位耗时 | 不能用单张模型耗时代替 |
| 持续吞吐 | 连续运行稳定处理速率 | 关注峰值队列和积压趋势 |
| 静默错配率 | 错配但未被系统发现的事件比例 | 目标必须为 0 |
| 回退成功率 | 异常时回到稳定版本/原 AOI 的成功比例 | 反映生产可恢复性 |

### 7.2 目标值与事实值分开

| 口径 | 可以怎么说 |
|---|---|
| 已验证事实 | “在某设备、某产品、某样本量下实测……” |
| 历史成果 | “历史项目曾达到 42% -> 9%，不直接代表本现场结果” |
| 设计目标 | “方案目标为……，需 PoC/影子运行验收” |
| 尚未验证 | “当前缺少设备接口或连续生产数据，不能宣称上线效果” |

### 7.3 价值链

```text
AI 复判
 -> 误报过滤/REVIEW 分流
 -> 人工复检量下降
 -> 复检等待和停线减少
 -> 新机种导入更快
 -> 缺陷按工站/批次沉淀
 -> 高缺陷率预警与异常报告
 -> 工艺、设备和模型共同改进
```

### 7.4 ROI 计算方式

```text
年度净收益 = 减少复检人力成本 + 减少停线损失 + 导入加速带来的产能收益
             - GPU/存储/运维/标注/模型迭代成本

投资回收期 = 项目总投入 / 月均净收益
```

准备面试时至少能说明：统计周期、样本量、人工复检单价、停线成本、部署成本和收益归属，不要只报一个未经核算的“节省金额”。

---

## 8. 高频专业问题（精简回答）

### Q1：为什么不直接替换 AOI？

原 AOI 承担基础检测和机械安全边界。旁路复判可先处理 NG/低置信度样本，影子验证后再按类别开放自动 PASS；AI 故障不阻塞原设备。

### Q2：最大技术难点是什么？

不是把 YOLO 跑起来，而是证明每张 2D、3D 和 AOI 数据属于同一个组件、槽位和批次。错误关联会让输出看似合理但业务结论完全错误。

### Q3：为什么使用 YOLOv8？

需要定位时，YOLOv8 能同时输出类别、置信度和框，且 ONNX/TensorRT 生态成熟；已有稳定 ROI 时会先比较轻量分类模型，不为使用 YOLO 而使用 YOLO。

### Q4：11 组光源如何输入？

先做光源消融，筛选 3-4 组组成标准 3 通道；若损失明显，再评估四通道首层改造或结果/特征融合。最终由召回、显存和持续吞吐决定。

### Q5：3D 是否直接拼到 YOLO？

不直接拼。3D 球高、共面性、翘曲走后融合，并保留物理阈值护栏；3D 缺失进入 `2D_ONLY`，默认不自动 PASS。

### Q6：乱序或重复文件怎么办？

按 Source Key 原子 Upsert，事件先进入 `RECEIVED/COLLECTING`，在超时内等待附件；附件 Hash、唯一约束和输入指纹保证幂等，超时或身份缺失进入隔离。

### Q7：如何确定 PASS/FAIL/REVIEW？

身份或输入质量异常先 REVIEW；缺陷证据充分或 3D 硬规则触发 FAIL；只有输入完整、置信度校准通过、策略允许的高置信度正常样本才能 PASS。

### Q8：如何评价模型？

除 mAP 外，按产品/缺陷/设备/批次报告召回率、误报过滤率、自动 PASS 漏放率、REVIEW 比例、置信区间和校准误差。

### Q9：为什么 Agent/RAG 不放实时路径？

实时路径需要低延迟和确定性，Agent/RAG 更适合知识检索、人工复核辅助、漂移分析和异常报告等异步流程；即使 Agent 不可用，本机检测仍可继续。

### Q10：怎么从 PoC 上线？

数据审计 -> 离线 PoC -> 单机影子运行 -> 受控单机上线。每一阶段有进入/退出条件，任何关联、质量或性能门槛未达标都不扩大自动 PASS 范围。

### Q11：如何防止数据泄漏？

按 Tray、批次、设备和时间分组切分训练/验证/盲测，不能随机拆相邻图片；生产盲测集独立保留。

### Q12：模型怎么回退？

模型、TensorRT 引擎、阈值、策略和 3D 规则组成版本化发布包；新版本先影子运行，出现差异、漂移、积压或异常时缩小自动化或回退稳定版本。

### Q13：服务宕机怎么办？

AI 旁路停用并记录告警，原 AOI 继续生产；恢复后可根据事件状态补处理，不用未验证结果自动放行。

### Q14：如何处理未知缺陷？

使用 `UNKNOWN/REVIEW`，保留原图、3D、模型版本和理由码，进入人工复核与样本回流，不把未知类别当作正常品。

### Q15：如何证明没有静默错配？

连续样例审计 + 乱序/重复/延迟/断网/重启故障注入 + 人工抽检，要求可验证事件 Source Key 一致率 100%、静默错配率 0。

---

## 9. AI 产品经理追问题库

### Q16：为什么首期只做 2-3 类缺陷？

先选择误报量最大、边界相对明确、样本足够且收益可量化的类别跑通闭环，再扩展低频和未知缺陷。首期目标是证明交付路径，不是堆类别数量。

### Q17：客户只要降误报，如何防漏放？

把自动 PASS 设为受保护区域，以漏放率和缺陷召回作为硬门槛；不确定样本进入 REVIEW，不能用整体 Accuracy 掩盖风险。

### Q18：高缺陷率预警如何避免误报？

采用最小样本量、滑动窗口、连续异常次数、历史基线和关键缺陷优先级；告警需要去重、恢复、确认人、处置时长和异常报告，不直接自动停线。

### Q19：如何推进跨团队落地？

用接口清单、字段字典、缺陷字典、金标准流程和阶段门禁统一口径；设备负责导出和时间源，质量负责缺陷边界和复核，IT/MES 负责网络与回写，AI 负责模型和服务。

### Q20：如何定义项目成功？

同时看业务结果、质量边界、产线约束和治理能力：误报过滤/复检量、漏放率/召回、P95/吞吐/积压、可追溯/回退/人工闭环。

### Q21：异常报告由谁负责？

系统自动生成事实层：时间窗、工站、产品、缺陷分布、代表性图片、3D 指标、模型版本和趋势；根因与处置结论由质量/工艺工程师确认，AI 不替代责任审批。

---

## 10. 典型故障场景回答

| 场景 | 系统动作 | 面试关键词 |
|---|---|---|
| 文件未写完 | 等待稳定、重试、超时隔离 | 不读半文件 |
| 3D 缺失 | `2D_ONLY` 或 REVIEW | 默认禁止自动 PASS |
| Tray/Slot 不一致 | REVIEW/INVALID，触发告警 | 不自动融合 |
| 同一事件重复提交 | 数据库唯一约束 + 输入指纹 | Redis 不是事实来源 |
| 设备重启计数器归零 | 使用 DeviceSessionID/boot_id | 防 Source Key 碰撞 |
| 新模型异常 | 停止扩大自动化或回退 | 结果可追溯 |
| GPU/服务/数据库异常 | AI 旁路停用，原 AOI 继续 | 旁路不阻塞 |
| 未知缺陷 | `UNKNOWN/REVIEW` | 进入人工闭环 |
| 工站缺陷率升高 | 窗口统计 + 证据报告 | 先复核，不直接停线 |

---

## 11. 缺陷报表、工站推送与异常报告扩展

### 11.1 数据口径

每个推理事件至少沉淀：

```text
event_uuid、device_id、product_id、batch_id、station_id、tray_id、slot_index、surface
defect_code、ai_confidence、ai_decision、model_version、policy_version
human_label、review_status、created_at、decision_reason_codes
```

### 11.2 报表分层

- **看板层**：总检测数、PASS/FAIL/REVIEW、误报过滤率、缺陷 Top N、工站良率。
- **分析层**：按产品、批次、工站、班次、Tray 位置、光源和模型版本切片。
- **追溯层**：从异常指标回到 event_uuid、原始文件、3D 摘要、模型/策略版本和人工结论。

### 11.3 高缺陷率预警规则

建议使用“最小样本量 + 滑动窗口 + 历史基线 + 连续异常”组合：

```text
窗口样本数 >= N
且 缺陷率 > max(历史基线 + 3σ, 业务绝对阈值)
且 连续 K 个窗口异常
=> 生成工站预警
```

关键缺陷可单独配置更严格阈值。预警先通知工艺/设备/质量责任人，不直接停线；告警需有去重、确认、恢复和处置时长。

### 11.4 异常报告内容

```text
异常编号、工站/设备、时间窗口、产品/批次、样本量、缺陷率
缺陷分类 Top N、代表性图片、3D 量测摘要、Tray Map 热点
原 AOI 与 AI 差异、模型/策略版本、数据质量状态
可能原因候选、建议核查项、责任人、确认结论、关闭时间
```

Agent/RAG 服务负责检索证据并生成报告初稿；根因和处置结论由质量/工艺人员确认。

---

## 12. 面试反问清单

### 应聘 AI 应用工程师

1. 现场是否已有稳定的 Inspection ID、Tray、Slot 和正背面字段？
2. 推理服务部署在什么边缘硬件上？是否有真实峰值吞吐和 P95 数据？
3. 当前更重视数据适配、模型召回、端到端延迟还是 MES 集成？
4. 是否已有影子、灰度、审批和回退机制？
5. 质量团队如何定义自动 PASS 的漏放边界？

### 应聘 AI 产品经理

1. 当前最优先解决误报、漏检、导入周期还是工站闭环？
2. 自动 PASS 最终由质量、设备还是生产团队批准？
3. 新机种样本准备、标注和阈值验证由谁负责？
4. 工站预警是通知、复核建议还是需要联动设备？
5. 项目成功按模型指标、复检量、产能收益还是质量成本衡量？

---

## 13. 面试前一周准备清单

### 必须补齐的项目事实

- 试点设备型号、相机分辨率、FOV、光源数量和 3D 指标。
- 2D、3D、AOI、Tray Map 的真实文件/接口格式和身份字段。
- Source Key 字段、设备重启策略、冲突处理和异常状态。
- 模型是分类还是检测，缺陷类别、样本量和标注规范。
- 训练/验证/盲测如何按 Tray、批次、设备和时间隔离。
- PASS/FAIL/REVIEW 的阈值、理由码和人工复核流程。
- TensorRT 版本、FP16/INT8 选择、边缘工控机实测 P95。
- 影子运行时长、回退演练结果和自动化开放范围。
- 历史成果、当前目标和已验证指标的证据来源。

### 不能含糊的三个问题

1. “你做过还是设计过？”——按实际情况回答，不把方案写成生产上线。
2. “42% 到 9% 是怎么测的？”——说明产品、时间窗、样本量、金标准和统计口径。
3. “为什么能保证不串件？”——先讲 Source Key 和状态机，再讲时间戳只是校验项。

---

## 14. 最后一页速记卡

1. 旁路复判，不替换原 AOI。
2. 先解决高成本误报，未知样本保留人工通道。
3. YOLOv8 不是原生 11 通道，先筛光源再决定融合。
4. 稳定 ROI 优先分类，需要定位才用检测。
5. 3D 做后融合和规则护栏，不直接硬塞进 YOLO。
6. 时间戳用于校验，Source Key 用于身份关联。
7. 数据不完整或身份不明，不能自动 PASS。
8. 单张推理时间不等于产线端到端吞吐。
9. Agent/RAG 管异步分析与报告，不放实时关键路径。
10. 工业 AI 的核心是质量边界、回退和可审计，不只是模型精度。

---

## 15. 现有资料的使用顺序

1. 先看本手册，建立完整叙事和岗位表达。
2. 再看《简历项目描述与面试讲解》，挑选对应岗位的简历版本。
3. 最后看《面试专业题库与落地复盘》，逐题练习追问和故障场景。
4. 面试前回到 V3.5 数据关联专章，核对字段、状态、幂等和生产门禁，确保回答与技术基线一致。

## 16. 统一版面试 10 问 10 答

本轮新增的项目架构、团队分工、人工复核与人在回路、YOLOv8s/v8x、TensorRT/TensorFlow、测试参数与结果等问题，已统一整理为独立速答稿：

[《PIS-IN_AOI_AI智能质检_面试10问10答_统一版》](PIS-IN_AOI_AI智能质检_面试10问10答_统一版.md)

使用顺序：先背统一版 10 问，再根据面试官追问回到本手册第 4、5、7、8、9、11 节展开。
