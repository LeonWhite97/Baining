# PIS-IN AOI AI 智能质检系统

## 面试专业题库与落地复盘

---

## 1. 项目整体解读框架

面试时可以用六层结构解释项目：

```text
业务层：误报、复检、漏放、新机种导入、设备节拍
数据层：2D多光源、3D、AOI、Tray Map、批次和工站元数据
算法层：ROI分类/YOLO检测、光源筛选、3D后融合、三态决策
服务层：FastAPI、TensorRT、批处理、幂等、超时和回退
治理层：独立Agent/RAG服务、人工复核、金标准、影子测试、模型发布
价值层：误报过滤、复检下降、质量追溯、工站预警、异常闭环
```

项目不是“训练一个 YOLO 模型”，而是把模型放入一个可以在产线运行、失败可见、结果可追溯的质量决策系统。

---

## 2. 高频专业问题与参考回答

### Q1：这个项目解决的核心问题是什么？

参考回答：

> 核心问题不是设备没有检测能力，而是传统规则在材料、工艺和光照波动下容易误报，设备产生的多光源、3D 和 AOI 数据也没有形成统一的可验证决策链路。当前方案部署在 AOI 工控机上，由操作员或本机软件触发采集，AI 在原 AOI 之后做本机旁路复判，优先过滤高成本误报，同时通过三态决策、数据关联校验和回退机制保护漏放风险。

### Q2：为什么不直接替换原 AOI？

参考回答：

> 原 AOI 已经承担设备基础检测和机械流程，直接替换会扩大质量和设备风险。当前不接入自动化 Handler，而是在同一台 AOI 工控机上完成采集、复判、复核和报表。影子模式先验证 AI 与人工金标准的差异，再针对高置信度、低风险类别逐步开放本机自动 PASS；AI 模块异常时保留原 AOI 结果并转人工复核，不向外部设备发送错误控制信号。

### Q3：为什么使用 YOLOv8？

参考回答：

> YOLOv8 适合在需要缺陷定位时同时输出类别、置信度和边界框，推理生态和 TensorRT 导出路径成熟。已有稳定 AOI ROI 时，我不会强行使用整图检测，而是优先用轻量 ROI 分类模型；只有 ROI 不可靠、一个画面多个缺陷或需要定位时才用 YOLO 检测。也就是说，模型选择由 ROI 和验收指标决定，而不是为了使用 YOLO 而使用 YOLO。

### Q4：YOLO 如何处理 11 组光源？

参考回答：

> YOLOv8 不是原生 11 通道模型。首期先对 11 组光源做可分性、稳定性和消融实验，筛选 3-4 组最有价值的光源，构造成标准 3 通道输入，从而复用预训练权重并降低 TensorRT 适配风险。如果 3 通道损失明显，再评估 4 通道第一层改造、结果级融合或特征级融合。光源数量的选择最终由召回率、显存和持续吞吐共同决定。

### Q5：为什么不把 3D 数据直接拼到 YOLO 输入？

参考回答：

> 3D 数据本质上是球高、共面性、翘曲等数值特征，不是与 2D 像素同构的图像。直接拼接会增加模型结构和部署复杂度，也不一定保留物理意义。我的做法是 YOLO 提取 2D 视觉证据，3D 通过数值模型或后融合层参与最终风险评分，同时保留物理阈值作为安全护栏。

### Q6：多光源、3D 和 AOI 如何保证对应的是同一组件？

参考回答：

> 不依赖文件修改时间猜测。系统优先使用设备原生 Inspection ID；否则使用 DeviceID、DeviceSessionID、InspectionSequence、TrayID、SlotIndex 和 Surface 构造确定性 Source Key。R/G/B/RING 是附件属性，汇聚到同一个 event_uuid。文件进入后经过文件完整性、结构、关联、采集时间和幂等五级校验。无法证明身份的数据进入 QUARANTINED 或 REVIEW，不能用邻近文件补位。

### Q7：如果文件乱序到达怎么办？

参考回答：

> 文件先进入 `RECEIVED/COLLECTING` 事件，系统在 `assembly_timeout` 内等待预期附件。每次到达都用原子 Upsert 定位事件，并在事务中写入附件、重新计算光源集合和状态。文件完整且所有必需数据到齐后才进入 READY；超时进入 EXPIRED 或 REVIEW。乱序只影响到达顺序，不改变 Source Key。

### Q8：如果同一时间戳对应多个组件怎么办？

参考回答：

> 同一时间戳不能作为唯一身份。必须从文件名、文件头、设备日志或 Manifest 中获取 Slot Index、区域号、扫描序号或组件序号。如果这些字段不存在，系统不能恢复真实归属，就进入 QUARANTINED，而不是用时间戳或文件顺序猜测。

### Q9：如何防止同一文件被重复推理？

参考回答：

> 数据库是最终幂等依据。物理事件通过 source_key_hash 唯一，附件通过 event_uuid、data_type、light_id 和 file_hash 去重，推理结果通过 event_uuid、model_version、policy_version 和 input_fingerprint 去重。Redis 如使用只做快速缓存，不作为最终事实来源。并发情况下使用事务 Upsert 和事件行锁。

### Q10：如果 3D 文件丢失怎么办？

参考回答：

> 不能简单把置信度乘一个系数。若 2D 身份已确认，可以使用独立校准的 2D_ONLY 模式，但默认禁止自动 PASS；身份不完整或数据错位则直接 REVIEW/QUARANTINED。2D_ONLY 是否开放自动放行，需要独立盲测证明召回率和漏放率满足质量边界。

### Q11：PASS、FAIL、REVIEW 如何确定？

参考回答：

```text
输入身份或质量异常 -> REVIEW
缺陷证据充分或3D硬规则触发 -> FAIL
原AOI为NG/低置信度，输入完整，模型置信度高，规则允许 -> PASS
其他中间区域 -> REVIEW
```

> 模型置信度不能直接等于最终概率，需要在独立验证集上做校准，并按产品和缺陷类别搜索阈值。PASS 保护漏放风险，FAIL 保护误判风险，中间区域留给人工复核。

### Q12：如何评价模型，不只看 mAP？

参考回答：

> 工业质检中 mAP 只能作为模型参考指标。我会按产品、缺陷类别、设备、批次、Tray 位置和班次报告缺陷召回率、自动 PASS 漏放率、原 AOI 误报过滤率、REVIEW 比例、置信区间和置信度校准。自动 PASS 漏放率是生产质量门禁，不能被整体 Accuracy 掩盖。

### Q13：如何划分训练集和测试集？

参考回答：

> 不能随机按单张图片划分，因为同一 Tray 或同一批次的相邻图像会泄漏到训练和测试。应按 Tray、批次、时间、设备、班次分组隔离，并单独保留生产盲测集。低频缺陷使用主动学习和难负样本挖掘，不用复制粘贴制造不真实缺陷。

### Q14：为什么 LangGraph 不放在实时推理链路？

参考回答：

> 实时产线更需要低延迟、确定性、可降级和少故障点。LangGraph 的价值在于管理有状态且允许人工介入的离线流程，例如人工复核、金标准回流、训练评估、模型审批、影子发布、漂移处理和异常报告。实时链路由 FastAPI、数据适配器、TensorRT 推理和规则决策组成。

### Q15：FastAPI 在系统中做什么？

参考回答：

> FastAPI 负责设备输入、健康检查、单组件/Tray 级请求、结果回写和模型管理接口，同时承载数据适配层的校验入口。推理引擎应常驻进程，不能每次请求加载模型；服务侧要有幂等键、批量上限、超时、限流、重试和健康检查。模型更新只能接受受控注册版本，不能直接接受任意文件路径。

### Q16：TensorRT 如何验证不影响产线？

参考回答：

> 先把 PyTorch 模型导出 ONNX，再在真实边缘工控机上验证 TensorRT FP16。INT8 只有在校准集和盲测精度不下降后才考虑。验收不只测单张推理，而是测图像读取、文件稳定、预处理、GPU 推理、排队、3D 融合和结果回写的端到端 P95、持续吞吐、峰值队列和超时比例。

### Q17：模型如何热更新和回退？

参考回答：

> 模型、TensorRT 引擎、阈值、决策策略和 3D 规则组成版本化发布包，先离线评估，再在单机影子模式记录新旧差异，审批后切换。结果保存 model_version、policy_version 和 input_fingerprint。健康检查、差异率、漂移或积压异常时关闭本机自动 PASS、切回稳定版本或全部转 REVIEW，原 AOI 检测结果仍可追溯。

### Q18：如何从 PoC 走到生产？

参考回答：

```text
数据审计：接口、身份字段、金标准和样本口径
离线 PoC：高频缺陷、盲测和阈值校准
影子运行：AI 只建议，原 AOI 控制产线
单机受控决策：按产品和缺陷类别逐步开放本机自动 PASS
```

> 每个阶段有进入和退出条件。任何阶段失败都回到数据、模型或接口处理，不以缩短工期为由跳过盲测和影子运行。

### Q19：项目的最大技术难点是什么？

参考回答：

> 最大难点不是把 YOLO 跑起来，而是证明模型输入属于正确的组件、正确的槽位和正确的批次。工业现场的错误关联会让模型输出看起来合理，但业务结果完全错误。因此我把确定性主键、文件状态机、事务幂等、异常隔离和静默错配率验收放在模型之前。

### Q20：如何扩展到缺陷报表和工站预警？

参考回答：

> 每个推理事件已经记录 defect_code、置信度、产品、批次、工站、Tray、模型版本和人工确认结果，因此可以按产品、批次、工站、班次和时间窗口生成分类报表。高缺陷率预警使用确定性统计规则，例如最小样本量、历史基线、连续异常窗口和关键缺陷计数。LangGraph 可以异步汇总代表性图片、3D 指标和 Tray Map 生成异常报告初稿，但不直接替代质量工程师的根因确认。

---

## 3. AI 产品经理追问题库

### Q21：如何定义项目成功？

回答框架：

```text
业务结果：误报过滤率、人工复检量、新机种导入周期
质量边界：缺陷召回率、自动PASS漏放率、UNKNOWN处理率
产线约束：持续吞吐、端到端P95、峰值积压、回退成功率
治理能力：数据可追溯、模型可回退、人工复核闭环、漂移告警
```

### Q22：为什么先做 2-3 类缺陷，而不是一次做全量？

参考回答：

> 首期要证明闭环，而不是堆功能。优先选择误报量最大、边界相对明确、样本足够、质量收益可量化的 2-3 类缺陷。先把数据、金标准、阈值、人工复核、影子和回退跑通，再扩展低频或未知缺陷。

### Q23：客户只关心降误报，如何防止漏放？

参考回答：

> 将自动 PASS 设计成受保护区域，不追求模型整体 Accuracy 最大化。PASS 阈值按漏放边界校准，缺陷证据和 3D 硬规则可直接 FAIL，低置信度和未知样本进入 REVIEW。验收同时报告误报过滤率和自动 PASS 漏放率，不用一个综合指标掩盖质量风险。

### Q24：如何管理设备、质量、IT/MES 和 AI 多方需求？

参考回答：

> 用接口清单、字段字典、缺陷字典、金标准流程和阶段门禁统一口径。设备团队负责本机采集接口和时间源，质量团队负责缺陷边界和复核，AI 团队负责模型与服务；MES 如接入只做异步数据同步，由 IT/MES 团队负责。所有模型、阈值、本机自动 PASS 范围和接口变更都保留审批记录。

### Q25：高缺陷率预警如何避免误报？

参考回答：

> 先设置最小样本量，再结合产品、工站和班次基线；使用滑动窗口、连续异常次数和关键缺陷优先级，而不是单个组件就报警。预警只通知和推动复核，不自动停线。告警要有去重、恢复、确认人、处置时间和报告闭环。

### Q26：如何计算 ROI？

参考回答：

```text
年度收益 = 减少的复检人数 × 综合人力成本
         + 减少的停线损失
         + 换线加速带来的产能收益
         - GPU、存储、运维和模型迭代成本

回收期 = 项目总投入 / 月均净收益
```

> 计算时区分保守、基准和积极三种场景，并由质量、生产、财务共同确认输入，不把历史项目 ROI 直接套用到新现场。

---

## 4. 生产故障场景回答模板

| 场景 | 系统动作 | 面试回答重点 |
|---|---|---|
| 2D 文件未写完 | 等待稳定、重试、超时隔离 | 不读取半文件，不用邻近文件补位 |
| 3D 文件缺失 | 2D_ONLY 或 REVIEW | 默认禁止自动 PASS，独立阈值和盲测 |
| Tray/Slot 不一致 | REVIEW/INVALID | 不能自动融合，告警设备和 IT |
| 同一事件重复提交 | 数据库唯一约束和输入指纹 | Redis 只做缓存，数据库是事实来源 |
| 设备重启计数器归零 | 使用 DeviceSessionID | 防止 Source Key 碰撞 |
| 新模型异常 | 停止扩大自动化或回退 | 原 AOI 不受影响，结果版本可追溯 |
| GPU/服务宕机 | 本机 AI 旁路停用，事件转 REVIEW | 保留原 AOI 结果，恢复后补处理，不产生自动 PASS |
| 未知缺陷 | UNKNOWN/REVIEW | 不把未知类别自动 PASS |
| 预测缺陷率突然升高 | 工站预警和证据报告 | 先核查质量、设备、光源和模型漂移 |

---

## 5. 工程指标与专业词汇

| 名称 | 含义 | 面试时怎么说 |
|---|---|---|
| 误报率 | 正常品被判为缺陷的比例 | 目标是减少人工复检，但不能牺牲漏放边界 |
| 漏放率 | 真实缺陷被 AI 自动 PASS 的比例 | 自动 PASS 的首要质量门禁 |
| 误报过滤率 | 原 AOI 误报中被 AI 正确过滤的比例 | 以人工复核金标准计算 |
| REVIEW 比例 | 进入人工复核的样本占比 | 是安全和自动化收益之间的平衡指标 |
| P95 延迟 | 95% 请求在该时间内完成 | 比平均延迟更能反映产线尾部延迟 |
| 持续吞吐 | 连续生产期间稳定处理的事件速率 | 不能用单张模型时间替代 |
| Shadow | 新模型只记录建议不控制产线 | 用于发现跨批次和新旧模型差异 |
| Drift | 输入或预测分布随时间变化 | 触发重新采样、评估和发布 |
| 幂等 | 同一事件重复提交不会产生重复结果 | 由 Source Key、Hash 和版本约束保证 |
| 静默错配 | 数据错配但系统没有发现 | 生产关联验收必须为 0 |

---

## 6. 反问面试官的问题

### 应聘 AI 应用工程师

1. 现场设备数据是否已经具备稳定的 Inspection ID、Tray、Slot 和正背面字段？
2. 推理服务部署在什么边缘硬件和操作系统上？是否有真实峰值吞吐数据？
3. 当前更看重模型召回、端到端延迟、数据适配还是 MES 集成？
4. 模型发布是否有影子、灰度、审批和回退机制？
5. 质量团队是否有明确的金标准和自动 PASS 漏放边界？

### 应聘 AI 产品经理

1. 当前客户最优先解决的是误报、漏检、导入周期还是工站闭环？
2. 产品上线由质量、设备、IT/MES 哪个团队最终批准？
3. 新机种导入的样本准备、标注和阈值验证由谁负责？
4. 工站预警希望通知、停线建议还是自动联动设备？
5. 项目成功是按模型指标、人工复检量、产能收益还是质量成本衡量？

---

## 7. 最后一页速记卡

面试前记住这 10 句话：

1. 这是 AOI 工控机单机旁路复判，不是 Handler 自动化控制，也不是替换 AOI。
2. AI 先解决高成本误报，未知样本保留人工通道。
3. YOLOv8 不是原生 11 通道模型，先筛光源再决定融合。
4. 有稳定 ROI 时优先分类，需要定位时才检测。
5. 3D 用后融合和物理规则，不直接硬塞给 YOLO。
6. 时间戳用于校验，Source Key 用于身份关联。
7. 数据不完整或身份不明，不能自动 PASS。
8. 单张推理时间不等于产线端到端吞吐。
9. LangGraph 管离线治理，不放入实时关键路径。
10. 工业 AI 的核心是质量边界、回退和可审计，不只是模型精度。

### Q27：为什么现阶段不接入自动化 Handler？

参考回答：

> 项目组现阶段优先验证单机检测闭环，把资源集中在图像采集、Source Key、YOLO/TensorRT、人工复核和缺陷报表。Handler 集成会额外引入 TCP 握手、跨设备状态机、实时 BIN 回传、硬件在环和错误分选责任边界，扩大 PoC 风险与联调周期。因此我们保留经过设计的 Handler 接口资产，但通过 `HANDLER_INTEGRATION_ENABLED=false` 默认关闭，待单机质量门禁稳定且业务重新确认自动分选需求后再独立立项。
