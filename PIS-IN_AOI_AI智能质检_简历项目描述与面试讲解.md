# PIS-IN AOI AI 智能质检项目

## 简历项目描述与面试讲解

> 使用说明：如果项目仍处于方案设计、PoC 或影子运行阶段，简历中应使用“设计、负责、搭建、验证、规划”，不要把未经过现场验收的目标写成已经上线的结果。42% -> 9%、2-3 天 -> 4 小时和年节约超过 200 万元属于历史项目成果或测算口径，除非有新的现场证据，不应表述为本次项目的无条件成果。

---

## 1. 项目一句话定位

针对 INTEKPLUS PIS-IN Series 半导体封装 AOI 设备误报率高、人工复检量大和新机种导入慢的问题，设计一套部署在 AOI 工控机上的单机 AI 复判系统，融合 2D 多光源图像、3D 量测和原 AOI 结果，通过 YOLOv8、TensorRT 适配边界、FastAPI、PostgreSQL 和独立 Agent/RAG 服务实现可追溯、可回退的质量决策闭环。当前不接入自动化 Handler。

## 2. 简历项目名称

推荐名称：

```text
INTEKPLUS PIS-IN AOI 智能质检与误报过滤系统
```

也可以根据岗位调整：

```text
面向半导体封装产线的 AOI 多模态 AI 复判与质量运营平台
```

### 2.1 技术栈

```text
PIS-IN AOI / R-G-B-RING / 3D / Tray Map
YOLOv8s / PyTorch / ONNX / TensorRT FP16（适配边界）
FastAPI / Pydantic v2 / SQLAlchemy 2 / Alembic
PostgreSQL 16 / pgvector / SQLite tests
React 18 / TypeScript / Vite / ECharts / Lucide
Docker Compose / Nginx / healthcheck / GPU overlay
独立 Agent/RAG 服务 / 确定性 Provider / 证据引用
```

### 2.2 架构类型

```text
单机容器化的模块化服务架构，不是严格的全微服务架构。
核心检测链路集中在 FastAPI 模块化服务中；Agent/RAG 作为独立辅助服务隔离；
前端、API、PostgreSQL、Agent/RAG 和模拟器通过 Docker Compose 部署在同一工控机。
```

### 2.3 个人职责与成果

- 负责需求指标化、数据关联方案、模块边界、三态决策和单机上线门禁。
- 推动 YOLOv8/TensorRT 适配、FastAPI、人工复核、报表预警和 Agent/RAG 集成。
- 设计 Source Key、状态机、隔离区、输入指纹和数据库唯一约束，控制错配与重复推理风险。
- 完成 8 个前端业务页面；API 63 条、Agent/RAG 6 条、simulator 2 条测试通过；Compose 与 PostgreSQL 迁移已验证。
- 主动收敛范围，将 Handler 自动化改为默认关闭的未来扩展，降低现阶段软硬件联调复杂度。

---

## 3. AI 应用工程师版简历描述

### 3.1 精简版

```text
针对 INTEKPLUS PIS-IN Series AOI 误报率高和新机种调试周期长的问题，设计单机 AI 复判系统。通过确定性 Source Key 关联 2D 多光源图像、3D 量测、原 AOI 结果和 Tray Map；采用 ROI 分类/YOLOv8 检测、3D 数值特征后融合和 PASS/FAIL/REVIEW 三态决策，使用 FastAPI + TensorRT 适配边界构建本地推理服务，使用独立 Agent/RAG 服务辅助复核和异常报告。重点解决多源数据错位、文件异步到达、模型幂等、单机影子运行和故障回退问题。
```

### 3.2 强调工程能力版

```text
- 设计 PIS-IN AOI 单机复判架构，实时链路采用 FastAPI + YOLOv8 + TensorRT 适配边界，异步链路采用独立 Agent/RAG 服务辅助样本复核、知识检索和异常报告。
- 建立 Device + Session + InspectionSequence + Tray + Slot + Surface 的确定性 Source Key，将 R/G/B/RING 等光源作为附件属性，解决 2D、3D、AOI 异步到达时的数据错配风险。
- 设计 inspection_events、data_attachment_links、inference_results 和 quarantine_events 数据模型，使用数据库唯一约束、事务 Upsert 和输入指纹实现幂等和版本共存。
- 设计 FULL、2D_ONLY、PARTIAL、QUARANTINED、INVALID 等降级模式，确保未知缺陷、数据身份不明和服务异常不自动放行且不阻塞原 AOI。
- 按持续吞吐、端到端 P95、队列积压、漏放率、误报过滤率和静默错配率建立现场验收口径。
```

### 3.3 有历史成果证据时的量化版

```text
历史项目中，系统将综合误报率由 42% 降至 9%，新机种导入周期由 2-3 天缩短至 4 小时，单次模型推理控制在 50ms 以内，并通过人工复核金标准、影子运行和版本回退保护漏放风险。上述指标为历史成果，不直接作为新客户现场承诺。
```

### 3.4 没有真实上线证据时的稳妥版

```text
完成 PIS-IN AOI 场景的技术方案、数据关联设计、YOLO/TensorRT 推理链路和单机验收方法设计，形成从数据审计、离线 PoC、单机影子运行到受控单机上线的实施基线；其中现场相机接口、模型指标和设备节拍需通过 PoC 进一步验证。
```

---

## 4. AI 产品经理版简历描述

```text
- 将 AOI 误报、人工复检、漏放风险、新机种导入周期和设备节拍转化为可验收的产品指标和阶段门禁。
- 设计“实时复判层 + 数据资产层 + 模型治理层”的产品架构，明确 AI 只做旁路复判，不接管 PIS-IN 机械流程。
- 规划缺陷分类报表、Tray Map 分布、工站高缺陷率预警、异常报告、人工复核回流和模型漂移监控，形成检测到质量闭环的产品路线。
- 组织质量、设备、IT/MES 和 AI 团队确认数据字段、金标准、漏放边界、影子运行时长和自动 PASS 审批机制。
- 建立从数据审计、离线 PoC、单机影子运行到受控单机上线的四阶段交付路线，并将历史结果与新现场基线严格区分。
```

## 5. 30 秒项目介绍

```text
这是一个面向半导体封装 AOI 的单机 AI 复判项目。操作员或本机 AOI 软件触发检测，PIS-IN 原 AOI 继续负责基础检测，AI 主要处理 NG 和低置信度样本。系统将 2D 多光源图像、3D 量测、原 AOI 结果和 Tray Map 通过确定性 Source Key 关联，使用 YOLOv8 检测并结合 3D 规则输出 PASS、FAIL、REVIEW。本地服务采用 FastAPI 和 TensorRT 适配边界，Agent/RAG 用于异步复核辅助和异常报告。项目重点是数据身份、单机节拍、故障回退和质量验收。
```

## 6. 3 分钟项目介绍

```text
项目背景是 PIS-IN Series AOI 在半导体封装外观检测中存在误报高、人工复检量大以及新机种调参周期长的问题。传统规则对材料、焊盘和光照波动比较敏感，但设备已经具备多光源、3D 量测、In-Tray 和 Tray Flipping 能力，问题在于这些数据没有形成可验证的 AI 决策链路。

我的设计原则是保留原 AOI 的安全边界并采用单机独立运行。实时路径包括本机数据适配、输入质量检查、YOLO/TensorRT 推理、3D 后融合和三态决策；异步路径通过独立 Agent/RAG 服务辅助人工复核、知识检索、漂移分析和异常报告。

数据工程是这个项目的关键。不能简单按文件修改时间把 2D、3D 和 AOI 结果拼起来，否则可能把组件 A 的 3D 数据关联到组件 B 的图像。系统使用 Device、设备会话、InspectionSequence、Tray、Slot 和正背面构成确定性 Source Key，R/G/B/RING 等光源作为同一事件的附件属性，并通过事务 Upsert、文件 Hash、附件时间戳和数据库唯一约束保证幂等和可追溯。身份缺失、文件不完整、时间超差和未知缺陷都不自动 PASS。

模型侧已有稳定 ROI 时优先使用轻量分类模型；没有可靠 ROI 或需要定位缺陷时使用 YOLO 检测。11 组光源不会直接作为原生 11 通道 YOLO 输入，而是先做数据审计和消融，选择 3-4 组有效光源，必要时再比较多模型或特征融合。3D 球高、共面性和翘曲作为后融合特征，同时保留物理规则护栏。

上线按数据审计、离线 PoC、单机影子运行和受控单机上线四阶段推进。验收指标不只看 mAP 或单次推理时间，还包括各类缺陷召回率、自动 PASS 漏放率、误报过滤率、REVIEW 比例、端到端 P95、持续吞吐、峰值队列和静默错配率。AI 服务异常时原 AOI 继续工作，模型和策略经过影子验证后再分产品和缺陷类别逐步开放本机自动 PASS。
```

---

## 7. STAR 项目故事

### Situation：背景

PIS-IN AOI 使用固定规则和阈值进行判定，面对焊盘氧化、锡膏厚度、材料和光照波动时容易产生误报；同时设备的 2D 多光源、3D 量测和 Tray Map 数据分散在不同输出中，缺少统一的数据关联和质量闭环。

### Task：任务

在不修改设备机械流程和原 AOI 基础安全逻辑的前提下，建设一个可旁路运行的 AI 复判系统，降低高成本误报，同时保证漏放风险、产线节拍、数据追溯和模型回退可控。

### Action：行动

1. 将业务目标转化为误报过滤率、自动 PASS 漏放率、REVIEW 比例、端到端 P95 和持续吞吐等验收指标。
2. 设计确定性 Source Key 和事件状态机，解决多源文件乱序、重复、延迟和身份缺失问题。
3. 选择 ROI 分类或 YOLO 检测模型，使用光源消融筛选有效通道，使用 3D 后融合和物理规则护栏。
4. 使用 FastAPI 封装实时服务，TensorRT 优化边缘推理，数据库唯一约束和输入指纹保障幂等。
5. 使用独立 Agent/RAG 服务辅助人工复核、知识检索、模型评估和异常报告。
6. 通过单机影子运行和受控单机上线逐步扩大 PASS 范围，不让 AI 故障阻塞原 AOI。

### Result：结果

如果已有真实验收数据，可以填写：

```text
历史项目综合误报率：42% -> 9%
新机种导入周期：2-3 天 -> 4 小时
人工复检量：按现场金标准填写
漏放率：按产品/缺陷类别填写，并附样本量和置信区间
端到端 P95：按真实边缘工控机填写
```

如果还没有新现场结果，应表述为：

```text
形成可进入 PoC 的数据适配、模型训练、影子运行和生产验收基线，历史成果用于证明路径可行，新现场指标等待连续生产数据重新基线化。
```

---

## 8. 面试时要主动讲清楚的边界

### 不要把以下说法讲绝对

| 容易被追问的说法 | 更严谨的表达 |
|---|---|
| YOLOv8 直接支持 11 通道 | YOLOv8 不是原生 11 通道模型，首期通过光源筛选转成标准 3 通道输入 |
| 单张推理小于 50ms 就满足产线 | 50ms 只是模型测量，最终以端到端 P95、持续吞吐和队列为准 |
| AI 替换 AOI | AI 是旁路复判层，原 AOI 和机械流程继续承担基础安全职责 |
| 3D 数据直接输入 YOLO | 3D 数值作为后融合特征，并保留物理阈值护栏 |
| 时间戳可以匹配数据 | 时间戳只做同事件校验，身份关联依赖确定性 Source Key |
| AI 可以发现所有未知缺陷 | 未知和低置信度样本进入 REVIEW，不把未知样本自动放行 |
| Agent/RAG 参与实时推理 | Agent/RAG 只用于异步分析、复核辅助和报告，不参与实时 PASS |
| 42% 一定能降到 9% | 42% -> 9% 是历史成果，新现场需按金标准重新基线化 |

---

## 9. 面试前必须准备的事实清单

不要只背架构图，应准备以下事实：

1. 试点设备型号、相机分辨率、FOV、光源数量和 3D 指标。
2. 2D、3D、AOI、Tray Map 的真实输出方式和身份字段。
3. Source Key 的字段、冲突处理和设备重启策略。
4. YOLO 是分类还是检测，选择理由和缺陷类别。
5. 训练集、验证集、盲测集如何按批次和 Tray 隔离。
6. 三态决策的 PASS、FAIL、REVIEW 条件。
7. 2D_ONLY、PARTIAL、QUARANTINED、INVALID 如何处理。
8. TensorRT 版本、FP16/INT8 选择和实际工控机性能。
9. 影子运行时长、回退机制和模型发布审批流程。
10. 历史成果、新现场目标和已经实际验证的指标分别是什么。
