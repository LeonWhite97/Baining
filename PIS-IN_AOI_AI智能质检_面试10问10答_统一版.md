# PIS-IN AOI AI 智能质检项目

## 面试 10 问 10 答（统一版）

> 适用岗位：AI 应用工程师、AI 产品经理、工业视觉质检解决方案岗位。
>
> 回答边界：当前项目采用 AOI 单机独立运行，不接入自动化 Handler。技术栈为 YOLOv8、TensorRT 适配边界、FastAPI、PostgreSQL、React、Docker Compose 和独立 Agent/RAG 服务。面试时应区分已验证事实、历史成果、设计目标和待现场验收项，不把方案指标直接说成生产实测结果。

### 1. 请介绍一下这个项目，解决了什么问题？

**回答：**

我做的是面向半导体封装产线的 PIS-IN AOI AI 单机复判系统。操作员或本机 AOI 软件触发检测，PIS-IN 在本机采集多光源与 3D 数据，AI 不替换原设备，而是复判原 AOI 的 NG、低置信度和重点样本，目标是减少误报和人工复检，同时保护漏放风险。

系统将 2D 多光源图像、3D 量测、原 AOI 结果和 Tray Map 关联到同一个检测事件，经过 YOLOv8/TensorRT 推理、3D 后融合和物理规则，输出 `PASS/FAIL/REVIEW`。结果在本机看板完成复核、缺陷报表、工站高缺陷率预警和异常报告；MES 只做可选异步同步，不参与实时判定。

项目价值不能只用模型精度衡量，还包括减少人工复检、缩短新机种导入、降低错配风险、保留审计证据，以及 AI 故障时不影响原 AOI 生产。

### 2. 系统核心模块和整体架构如何设计？模块之间如何交互？

**回答：**

项目可以拆成五个模块：本机数据适配与关联、AI 推理、规则决策、本地质量运营、模型治理。

实时链路是：

```text
本机操作 / PIS-IN 数据输出
→ 文件稳定性与字段解析
→ Source Key 关联和完整性校验
→ FastAPI 接口
→ YOLOv8/TensorRT 推理
→ 3D 后融合与物理规则
→ PASS/FAIL/REVIEW
→ PostgreSQL、Tray Map、本地看板和报表
```

架构不是严格全微服务，而是单机容器化的模块化服务架构。FastAPI、领域模型、推理编排和数据访问构成核心服务；Agent/RAG 独立部署，负责知识检索、复核辅助和异常报告。也就是说，实时服务负责确定性决策，Agent/RAG 负责可降级的异步流程。

如果面试官用“Agent 智能对话、审查、生成”来类比，我会说明：在本项目中对应的是“质量分析/复核编排、检测判定、异常报告生成”，但不会把合同审查模块名称硬套到 AOI 项目上。

### 3. 多光源、3D 和 AOI 结果如何保证属于同一个组件？

**回答：**

我不会按文件修改时间或文件先后顺序猜测归属，而是优先使用设备原生 Inspection ID；如果设备没有稳定 ID，则用 `DeviceID + DeviceSessionID + InspectionSequence + TrayID + SlotIndex + Surface` 生成确定性 Source Key，并通过规范化字段计算 `source_key_hash`。

R/G/B/RING 等光源是同一事件的附件属性，不放进事件主键。同一组件的多张光源图像共享一个 `event_uuid`。数据模型至少包含 `inspection_events`、`data_attachment_links`、`inference_results` 和 `quarantine_events`，分别管理物理事件、文件附件、多模型结果和身份缺失文件。

文件到达后要经过稳定性、字段、时间偏差、Hash 和完整性校验。乱序文件进入 `RECEIVED/COLLECTING` 等待，无法证明身份的文件进入隔离区，不能用相邻文件补位。生产验收要求静默错配率为 0。

### 4. YOLO 用什么模型？YOLOv8s 的精度够吗？为什么不直接用 YOLOv8x？

**回答：**

项目采用 YOLOv8 目标检测作为基线。若 AOI 已经提供稳定 ROI，我会同时评估轻量 ROI 分类；只有需要缺陷定位、一个视野中存在多个缺陷或 ROI 不稳定时，才使用 YOLO 检测。

模型选择上先用 YOLOv8s 建立基线，必要时比较 YOLOv8m 或 YOLOv8x。YOLOv8x 参数量和计算量更大，可能有更高精度潜力，但会增加显存、延迟、TensorRT 构建和回归验证成本。工业 AOI 不能只看 mAP，关键是每类缺陷 Recall、自动 PASS 漏检率、REVIEW 比例、端到端 P95 和持续吞吐。

YOLOv8s 是否够用必须以项目盲测结果决定，不能直接拿 COCO 公开 mAP 当现场精度。模型不足时，我会优先检查难例、输入分辨率、ROI 裁剪和光源消融，再决定是否升级模型规模。

11 组光源也不直接声称由 YOLO 原生支持。首期先做光源消融，筛选 3-4 组有价值的光源组成标准 3 通道；若召回损失明显，再评估四通道首层改造、结果级融合或特征级融合。

### 5. TensorRT 和 TensorFlow 有什么区别？项目为什么使用 TensorRT？

**回答：**

TensorFlow 是深度学习开发框架，主要负责模型构建、训练和通用部署；TensorRT 是 NVIDIA 的 GPU 推理优化引擎，主要负责生产推理加速，不能替代训练框架。

本项目的 YOLOv8 训练和推理生态以 PyTorch 为主，部署链路是：

```text
PyTorch 模型 → ONNX → TensorRT Engine → FastAPI 服务
```

优先采用 TensorRT FP16，通过算子融合、内核选择和低精度计算降低边缘 GPU 推理延迟。INT8 只有在代表性校准集和独立盲测证明缺陷召回率、漏放率没有恶化后才考虑。

如果使用 TensorFlow，也可以通过 ONNX 或 TF-TRT 接入 TensorRT；本项目选择 PyTorch 训练加 TensorRT 部署，主要是因为 YOLO 生态、ONNX 导出路径和 NVIDIA 边缘推理环境更匹配。

### 6. PASS、FAIL、REVIEW 如何决定？人工复核和人在回路有什么区别？

**回答：**

决策不是只看一个置信度：

```text
身份/文件/质量异常 → REVIEW
缺陷证据充分或 3D 硬规则触发 → FAIL
输入完整、模型校准通过、规则允许且风险可控 → PASS
```

3D 缺失可以进入独立的 `2D_ONLY` 模式，但默认不自动 PASS，必须用独立盲测证明质量边界仍然满足。

人工复核是一个具体动作，通常指人员查看图像、3D 数据和模型结果后给出确认；人在回路是更大的治理机制，指系统在不确定、低置信度、未知缺陷或异常情况下主动把决策交给人，并把人的结果回流到金标准、报表和模型迭代流程。人工复核是人在回路的一个环节，但两者不是同义词。

### 7. 测试参数和结果怎么讲？

**回答：**

测试分为离线模型、TensorRT 性能、端到端链路和故障注入四类。模型对比时固定数据集、输入尺寸、GPU、CUDA 和 TensorRT 环境，重点测试 YOLOv8s，并以 YOLOv8m/x 作为对照；部署重点使用 `batch=1` 和 FP16，输入尺寸可从 640 起步，必要时增加高分辨率或 ROI 对照。

离线指标包括各缺陷类别 Recall、Precision、F1、mAP、自动 PASS 漏检率、误报过滤率和 REVIEW 比例。性能指标包括预处理、推理、后处理及从文件接入到结果回写的端到端 P95、持续吞吐、峰值队列、超时比例和显存稳定性。

还要注入乱序、重复、延迟、文件不完整、断网、设备重启计数器归零和服务异常。真实项目数值必须以连续生产样本和盲测报告为准；如果尚未完成现场测试，我会明确说“测试方案和验收门槛已确定，项目实测结果待 PoC 验证”，不会把设计目标说成已上线结果。

### 8. 项目如何从 PoC 实施到生产上线？服务或模型异常怎么办？

**回答：**

实施路径是“数据审计 → 本机数据适配 → 离线 PoC → TensorRT 联调 → 单机影子运行 → 受控单机上线”。每个阶段都有进入和退出条件，重点验证设备字段、Source Key、一致性、盲测召回、自动 PASS 漏放率、端到端 P95、持续吞吐和故障恢复。

模型、TensorRT 引擎、阈值、决策策略和 3D 规则组成版本化发布包，记录 `model_version`、`policy_version` 和输入指纹。新版本先影子运行，出现差异异常、漂移、积压或超时就缩小自动化范围或回退稳定版本。

FastAPI、TensorRT、数据库或网络异常时，AI 旁路停止自动判定，原 AOI 继续生产；恢复后根据事件状态补处理，不能使用未验证结果自动放行。

### 9. 项目周期、团队配置和我的分工是什么？

**回答：**

项目周期为 2024 年 9 月到 2025 年 1 月，8 人按阶段参与：产品/AI 负责人 1、后端 2、前端 1、算法 2、测试 1、实施运维 1。落地过程分为需求与数据审计、PoC 与数据适配、单机联调与影子运行、受控上线和复盘交付。

这是按阶段参与的交付团队，不是 8 人全程同时投入。质量、设备和 IT/MES 作为业务协作方参与字段确认、金标准、网络和数据同步，不计入上述研发团队编制。

我的分工是 AI 应用与产品落地：把业务目标转成可验收指标，设计数据关联和模块边界，推进 YOLOv8/TensorRT 适配、FastAPI 接口、人工复核、Agent/RAG 和报表闭环，并组织算法、后端、前端、测试、实施以及质量/设备协作方完成单机联调和上线门禁。项目负责人侧重范围、资源和关键决策，我侧重方案细化、跨模块落地、风险闭环和交付证据。

当前可量化的个人成果是：完成 8 个前端业务页面、API 63 条测试、Agent/RAG 6 条测试、simulator 2 条测试，完成 Compose 与 PostgreSQL 迁移验证，并将 Handler 自动化改为默认关闭的可选扩展。真实模型精度和现场节拍仍按待验收口径回答。

### 10. 缺陷报表、工站推送和异常报告如何扩展？项目产生什么价值？

**回答：**

每个事件记录缺陷编码、置信度、产品、批次、工站、Tray/Slot、模型版本、原 AOI 结果和人工确认结果，因此可以按产品、批次、工站、班次和时间窗口生成缺陷分类报表与 Tray Map。

高缺陷率预警不能因为单个样本就触发，而应采用最小样本量、滑动窗口、历史基线、连续异常次数和关键缺陷优先级。告警推送到工站或质量人员后，系统生成包含异常时间窗、工站、缺陷分布、代表性图片、3D 指标、模型版本、趋势和处置状态的异常报告。根因和最终处置结论由质量/工艺工程师确认，AI 负责提供证据和编排。

项目价值可以从四个层面说明：减少原 AOI 误报和人工复检；保护关键缺陷不漏放；缩短新机种和规则调试周期；沉淀可追溯的数据资产，支持工艺、设备和模型共同改进。具体收益应以统计周期、样本量、人工复检成本和产线数据核算后对外公布。

## 面试前的统一口径

- 项目是旁路复判，不替换原 AOI。
- Source Key 负责事件身份，LightGroup 只是附件属性。
- YOLOv8s 是基线，不代表已经证明是最终最优模型；YOLOv8x 需要同条件实测。
- TensorFlow 是训练框架，TensorRT 是 NVIDIA 推理优化引擎。
- mAP 不能替代关键缺陷召回率和自动 PASS 漏放率。
- 人工复核是动作，人在回路是包含复核、回流和治理的机制。
- Agent/RAG 用于异步分析和报告，不放在实时关键路径。
- 当前是单机部署，不接入 Handler；Handler 接口只作为未来扩展储备。
- 未知、缺失、错配和低置信度样本默认进入 REVIEW 或隔离，不自动 PASS。
- 未经连续样本、盲测和影子运行验证的指标，必须表述为目标值或待验收项。
